[project]
name = "cli"
version = "0.1.0"
description = "qtcloud-connect CLI — provider API 客户端"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
]

[project.scripts]
qtcloud-connect = "main:main"
